from PIL import Image
import glob, os
import math
size = 128, 128
if __name__=="__main__":
    # # im = Image.open("1.jpg")
    # # im.rotate(90).show()   #displays it using an external viewer (usually xv on Unix, and the paint program on Windows).
    # #
    # # im.thumbnail(size)
    # # im.save(file + ".thumbnail", "JPEG")
    #
    # for infile in glob.glob("*.jpg"):
    #     file, ext = os.path.splitext(infile)
    #     im = Image.open(infile)
    #     print(im.filename)
    #     print (im.format)
    #     im.thumbnail(size)
    #     im.save(file + "2.jpg", "JPEG")
    print(str(hex(1000)))